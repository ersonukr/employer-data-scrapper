<%= form_for(@webfile) do |f| %>
  <% if @webfile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@webfile.errors.count, "error") %> prohibited this webfile from being saved:</h2>

      <ul>
      <% @webfile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :phone_number %><br>
    <%= f.text_field :phone_number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
